# StayLiquid

Sprinkler zone scheduling for Home Assistant. Pick any `switch.*` or
`valve.*` entities as zones, build watering programs from lawn growth-stage
presets (or a fully custom schedule), choose sequential or simultaneous
zone runs per program, and hit a rain-delay button from the dashboard.

See the **Documentation** tab for setup and usage.
